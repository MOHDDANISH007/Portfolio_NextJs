/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'cdn.jsdelivr.net',
      'raw.githubusercontent.com',
      'seeklogo.com', 
      'miro.medium.com'
    ],
  },
};

module.exports = nextConfig;
